# ── Base PUBLIQUE (partagée avec les autres groupes) ──────────────────
DB_HOST=127.0.0.1
DB_PORT=8889
DB_NAME=capteur_distance
DB_USER=root
DB_PASSWORD=root

# ── Base PRIVÉE (uniquement notre groupe) ────────────────────────────
DB_PRIVATE_HOST=127.0.0.1
DB_PRIVATE_PORT=8889
DB_PRIVATE_NAME=capteur_private
DB_PRIVATE_USER=root
DB_PRIVATE_PASSWORD=root

# ── Auth ─────────────────────────────────────────────────────────────
JWT_SECRET=CHANGE_THIS_SECRET
JWT_EXPIRES_IN=7d

# ── Serveur ───────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
DEMO_MODE=true
